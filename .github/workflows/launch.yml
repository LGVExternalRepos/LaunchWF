name: Trigger Target Workflow

on:
  workflow_dispatch:
    inputs:
      action:
          type: choice
          description: Action
          options: 
          - Scan
          - Upload
          default: Scan
      loops:
        description: 'Number of uploads'
        default: "3"
      target_version:
        description: 'Input a version (e.g., v1.0.0)'
        required: true


jobs:
  trigger:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0  # Shallow clones should be disabled for better relevancy of analysis
      
      - name: Upload File
        run: |
          declare -a Repos=(\
          "Mindustry" \
          "graylog2-server" \
          "dependency-track")

          ls -l 
          pwd
          POPO=$(cat kk.txt | base64 -w 0)
          echo $POPO

          curl -L \
            -X PUT \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ secrets.PAT_FOR_XYGENI_SCAN }}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/LGVExternalRepos/LaunchWF/contents/kk2.txt \
              -d '{"message":"my commit message","committer":{"name":"LGV","email":"luis.graci@xygeni.io"},"content":"${{ env.POPO }}"}'
          exit 1
        
      - name: Trigger Workflow in Another Repository
        run: |

          declare -a Repos=(\
          "Mindustry" \
          "graylog2-server" \
          "dependency-track")

          repo_owner="LGVExternalRepos" 
          event_type="trigger-workflow" 
          loops="${{ github.event.inputs.loops }}"
          version="${{ github.event.inputs.target_version }}"
          action="${{ github.event.inputs.action }}"
          event_type="${{ github.event.inputs.action }}"
          
          for i in "${Repos[@]}"
          do
           echo "$i"
           repo_name="$i"  
           curl -L \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ secrets.GH_PAT }}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/$repo_owner/$repo_name/dispatches \
            -d "{\"event_type\": \"$event_type\", \"client_payload\": {\"loops\": \"$loops\", \"version\": \"$version\", \"unit\": false, \"integration\": true}}"
          done

